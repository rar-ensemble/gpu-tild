This file fits a chi to simulation data using RPA analysis. 
It does so by modifing which sets of data are fed into the fit, creating the fit, 
and then determening which fit produces the lowest mean squared error when compared to the average of the simulation data.
